name: chatbucket-b2b-backend
region: blr
features:
  - buildpack-stack=ubuntu-22
services:
  - name: api
    git:
      repo_clone_url: https://github.com/coinexmarket/chatbucket_b2b_backend.git
      branch: main
    dockerfile_path: Dockerfile
    http_port: 8000
    instance_count: 1
    instance_size_slug: apps-s-1vcpu-1gb
    health_check:
      http_path: /health
      initial_delay_seconds: 30
      period_seconds: 30
      timeout_seconds: 5
      success_threshold: 1
      failure_threshold: 3
    envs:
      - key: ENVIRONMENT
        scope: RUN_TIME
        value: production
      - key: MONGODB_URI
        scope: RUN_TIME
        type: SECRET
        value: "${MONGODB_URI}"
      - key: JWT_SECRET
        scope: RUN_TIME
        type: SECRET
        value: "${JWT_SECRET}"
      - key: BILLING_WEBHOOK_SECRET
        scope: RUN_TIME
        type: SECRET
        value: "${BILLING_WEBHOOK_SECRET}"
      - key: STATUS_WEBHOOK_SECRET
        scope: RUN_TIME
        type: SECRET
        value: "${STATUS_WEBHOOK_SECRET}"
      - key: OPS_SECRET
        scope: RUN_TIME
        type: SECRET
        value: "${OPS_SECRET}"
      - key: EMAIL_BACKEND
        scope: RUN_TIME
        value: disabled
      - key: APP_BASE_URL
        scope: RUN_TIME
        value: https://chatbucket.business
      - key: TRUST_PROXY_HEADERS
        scope: RUN_TIME
        value: "true"
      - key: CORS_ORIGINS
        scope: RUN_TIME
        value: "https://chatbucket.business,https://www.chatbucket.business,https://chatbucket.chat,https://www.chatbucket.chat"
ingress:
  rules:
    - match:
        path:
          prefix: /
      component:
        name: api
domains:
- domain: api.chatbucket.business
  type: PRIMARY
